# YouTube Data API v3 credentials — copy to .env and fill in real values.
# Never commit the real .env or client_secret.json.
YOUTUBE_API_KEY=
YOUTUBE_CLIENT_SECRETS_FILE=client_secret.json

# Only needed for `channel-toolkit metadata --engine ai` / `batch --engine ai`
ANTHROPIC_API_KEY=
